#ifndef TCTOKENS_H
#define TCTOKENS_H

namespace toycalc {
  
   enum tokens{ ID, NUM, ADDOP, MULOP, RELOP, AND, OR, 
                WRITE, READ, IF, THEN, GOTO, ASSIGN, SKIP,
                COLON, SEMICOLON, LPAREN, RPAREN, EOFILE, NONE };

}

#endif
